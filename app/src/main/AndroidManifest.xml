<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kesteli.filip.sciencebrowser1">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="Sci News"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".web.WebActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".web.history.HistoryActivity"
            android:label="HISTORY"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".web.history.EurekaActivity"
            android:label="EUREKA"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".web.history.FavoriteActivity"
            android:label="FAVORITE"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme" />
        <activity android:name=".web.ClanciActivity" />
        <activity android:name=".aplikacije.realitycheck.RealityActivity" />
        <activity
            android:name=".aplikacije.education.EducationActivity"
            android:label="Edu Express"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".aplikacije.kzinicijativa.KZInicijativaActivity" />
        <activity android:name=".aplikacije.ScienceActivity" />
        <activity android:name=".aplikacije.motivatorapp.MotivatorActivity" />
        <activity android:name=".aplikacije.donacije.DonationActivity" />
        <activity android:name=".aplikacije.smartcity.SmartCityActivity" />
        <activity android:name=".aplikacije.democracy.DemocracyActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".aplikacije.kzinicijativa.DrinkMapsActivity"
            android:label="@string/title_activity_drink_maps" />
        <activity android:name=".aplikacije.realitycheck.RealityClanActivity" />
        <activity android:name=".aplikacije.realitycheck.RealityRezultatiActivity"></activity>
    </application>

</manifest>